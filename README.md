# 菊池球数字教学模型

将 FCC / BCC 展开图贴到 26 面多面体上，用于旋转观察菊池图案及极的位置。使用原生 HTML、CSS、JavaScript 和 Canvas；无需安装应用或运行后台服务。

**[打开在线教学模型](https://abigalelu.github.io/kikuchi-sphere-teaching/)**

## 观察方式

- 鼠标或单指拖动旋转；滚轮、双指或缩放按钮放大缩小。
- 切换 FCC / BCC，选择新增图纸或原版 PDF 对照，保持当前角度与缩放。
- 使用 `[001]`、`[110]`、`[111]` 预设方向；点击球体表面查看单面图案。
- 支持自动旋转、复位，以及方向键、加减键和 Home 键。

## 图纸与模型

`FCCpage1.jpg` 和 `FCCpage2.jpg` 两页构成一套 FCC 模型，`bcc_all2.jpg` 构成一套 BCC 模型。保留原图的线条、色彩、极标签和署名。

模型为 18 个正方形面和 8 个三角形面组成的多面体。它使用固定图案展示，不计算新的衍射关系，也不是用于实体打印的模型。

新图纸 BCC 原图每面约 330 像素，放大后的细节受源图分辨率限制。

## 文件

| 入口 / 文件 | 内容 |
| --- | --- |
| `index.html` | 默认入口，跳转到新图纸对照版 |
| `index-drawings.html` | 新图纸与原版 PDF 对照教学模型 |
| `index-original.html` | 第一版原 PDF 模型 |
| `viewer-drawings.js` / `model-drawings.js` | 对照版交互与面贴图对应关系 |
| `viewer.js` / `model-data.js` | 第一版交互与对应关系 |
| `assets/` | 原图与面贴图 |
| `source/reference.pdf` | 第一版参考 PDF |

下载整个仓库后也可直接打开 `index-drawings.html` 离线使用。所有资源使用相对路径，没有第三方 JavaScript 库或在线 API 依赖。

## GitHub Pages

发布源为 `main` 分支根目录，`.nojekyll` 表示直接发布静态文件。无需构建命令，也无需配置自定义域名。

本次公开版本保持原有渲染代码与图案。当前会在启动时加载四套贴图，共 104 张、约 63.4 MB；完整站点含源图约 78.1 MB，源 JPG/PDF 仅在主动打开时请求。首屏耗时与设备内存占用可能较高，尤其在手机或较慢网络上。后续可独立优化按需加载和图片传输。

已检查全部贴图文件、相对路径、四种组合的 Canvas 绘图、方向预设及切换时保留视角。此前的交互验证使用模拟事件和原生 Canvas；尚未完成真实手机浏览器的全面兼容性与性能验收。

## 出处

原图和衍生贴图的出处及许可见 [ATTRIBUTION.md](ATTRIBUTION.md)。本仓库独立存放教学模型，不包含 TEM 小程序业务代码或数据。
